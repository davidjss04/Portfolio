import React from 'react';
import { Typography } from '@mui/material';

const Logo = () => {
	return <Typography variant="h5" align='center' fontFamily='-Calibre'>{'<DAVID JSS/>'}</Typography>;
};

export default Logo;
